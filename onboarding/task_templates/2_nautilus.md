# Task 2: Nautilus Onboarding

Use the current Nautilus task from an existing project as the canonical content until the duplicated copies are consolidated. Students should obtain access, run the shared CIFAR-10 notebook and training script, demonstrate both a deployment and a job, verify CUDA use, and delete resources when finished.

Project authors should only change the namespace or resource requirements when their track genuinely needs different infrastructure.

